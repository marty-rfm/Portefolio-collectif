{
  "name": "portfolio-collectif-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}